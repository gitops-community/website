{
  "devDependencies": {
    "vuepress": "^1.5.0"
  },
  "scripts": {
    "dev": "vuepress dev docs --host ${DEVHOST:-localhost}",
    "build": "vuepress build docs --dest dist",
    "publish": "./publish.sh"
  }
}
